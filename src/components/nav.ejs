    <link href="https://cdn.jsdelivr.net/npm/@mdi/font@7.4.47/css/materialdesignicons.min.css" rel="stylesheet">

    <header class="header-nav-gen">
        <div class="header-content-nav">

            <div class="user-register">
                <span><strong><%= user.nombre_usuario %></strong></span>
            </div>
            <!-- Logo Section -->
            <div class="logo-nav">
            
                    <div class="logo-container-nav">
                        <a href="/api/index" onclick="showLoadingModal()">
                            <img class="logo-icon-nav" src="/images/logo_docsigned.png" alt="">
                        </a>
                    </div>

                <span class="logo-text-nav">Doc Signed</span>
            </div>
            
            <!-- Navigation Actions -->
            <div class="header-nav">

                <!-- Botón de Mis Solicitudes -->
                  <a href="/api/application" class="nav-btn solicitudes-requests-btn" id="solicitudesRequestsBtn" onclick="showLoadingModal()">
                    <!-- Icono de documentos/solicitudes -->
                    <span class="mdi mdi-account-details"></span>
                    <span>Solicitudes</span>
                    <!-- Badge con contador -->
                </a>
                <!-- Botón de Solicitudes Pendientes -->
                <a href="/api/pending/" class="nav-btn pending-requests-btn" id="pendingRequestsBtn" onclick="showLoadingModal()">
                    <!-- Icono de documentos/solicitudes -->
                    <span class="mdi mdi-file-sign"></span>
                    <span>Pendientes</span>
                    <!-- Badge con contador -->
                    <span class="pending-badge" id="pendingCount"><%= pendingDocuments %></span>
                </a>
                <!-- CreateFormat -->
                <a href="/api/createFormat" class="nav-btn create-format-btn" id="createFormatBtn" onclick="showLoadingModal()">
                    <!-- Icono de crear formato -->
                    <span class="mdi mdi-file-document-edit"></span>
                    <span>Formatos</span>
                </a>
                <!-- User Info -->
                <div class="user-info">
                    <a href="/api/userProfile" class="profile-link">
                        <button type="button" class="user-role" onclick="showLoadingModal()">👤</button>
                    </a>
                    
                    <!-- Logout Button -->
                    <form action="/api/auth/logout" method="post" class="logout-form" onsubmit="showLoadingModal()">
                        <button type="submit" class="logout-btn">
                            <svg class="logout-icon" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M16,17V14H9V10H16V7L21,12L16,17M14,2A2,2 0 0,1 16,4V6H14V4H5V20H14V18H16V20A2,2 0 0,1 14,22H5A2,2 0 0,1 3,20V4A2,2 0 0,1 5,2H14Z"/>
                            </svg>
                            <span>Salir</span>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </header>

    <!-- Modal de Carga Global para Navegación -->
    <div id="globalLoadingModal" class="global-loading-modal">
        <div class="loading-content">
            <div class="logo-container">
                <img src="/images/logo_docsigned.png" alt="DocSigned" class="loading-logo">
            </div>
            <h2>Cargando...</h2>
            <div class="loading-spinner">
                <div class="spinner"></div>
            </div>
            <p>Por favor espera mientras se carga tu página</p>
        </div>
    </div>

<script>
    // Función para mostrar el modal de carga global
    function showLoadingModal() {
        const loadingModal = document.getElementById('globalLoadingModal');
        if (loadingModal) {
            loadingModal.classList.add('active');
        }
    }
</script>

<script>
// Toggle Menu Móvil
document.addEventListener('DOMContentLoaded', function() {
  // Solo crear elementos si estamos en móvil
  const isMobile = window.innerWidth <= 768;
  
  // Crear botón hamburguesa
  const menuToggle = document.createElement('button');
  menuToggle.className = 'menu-toggle';
  menuToggle.id = 'menuToggle';
  menuToggle.setAttribute('aria-label', 'Toggle menu');
  menuToggle.innerHTML = '<span></span><span></span><span></span>';
  
  // Crear overlay
  const navOverlay = document.createElement('div');
  navOverlay.className = 'nav-overlay';
  navOverlay.id = 'navOverlay';
  
  // Insertar elementos
  const headerContent = document.querySelector('.header-content-nav');
  const headerNav = document.querySelector('.header-nav');
  
  if (headerContent && headerNav) {
    headerContent.insertBefore(menuToggle, headerNav);
    document.querySelector('.header-nav-gen').appendChild(navOverlay);
    
    // Función para toggle
    function toggleMenu() {
      const isActive = menuToggle.classList.toggle('active');
      headerNav.classList.toggle('active');
      navOverlay.classList.toggle('active');
      
      // Prevenir scroll solo cuando el menú está activo
      document.body.style.overflow = isActive ? 'hidden' : '';
    }
    
    // Event listeners
    menuToggle.addEventListener('click', toggleMenu);
    navOverlay.addEventListener('click', toggleMenu);
    
    // Cerrar menú al hacer clic en un link
    const navLinks = headerNav.querySelectorAll('a, button[type="submit"]');
    navLinks.forEach(link => {
      link.addEventListener('click', function() {
        if (window.innerWidth <= 768 && headerNav.classList.contains('active')) {
          toggleMenu();
        }
      });
    });
    
    // Cerrar menu al cambiar tamaño
    window.addEventListener('resize', function() {
      if (window.innerWidth > 768 && headerNav.classList.contains('active')) {
        menuToggle.classList.remove('active');
        headerNav.classList.remove('active');
        navOverlay.classList.remove('active');
        document.body.style.overflow = '';
      }
    });
  }
});
</script>